import React from 'react';
import styles from './styles.module.scss';

const Main: React.FC = () => (
  <section className={styles.wrapper}>
    <h1>Hello World</h1>
  </section>
);

export default Main;
